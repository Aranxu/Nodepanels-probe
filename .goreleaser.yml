project_name: nodepanels-probe
before:
  hooks:
builds:
  - env:
      - CGO_ENABLED=0
    id: "nodepanels-probe"
    binary: "nodepanels-probe-{{ .Os }}-{{ .Arch }}"
    goos:
      - linux
      - windows
    goarch:
      - 386
      - amd64
      - arm
      - arm64
    goarm:
      - 6
      - 7
archives:
  - format: binary